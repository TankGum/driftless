version: "3.9"
services:
  driftless:
    image: driftless:latest
    container_name: driftless-test
    restart: unless-stopped
    ports:
      - "8101:8000"
    env_file:
      - .env
    volumes:
      - /Users/tankgum/Documents/Workspace/driftless/credentials.json:/app/credentials.json:ro
